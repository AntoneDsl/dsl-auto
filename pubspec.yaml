name: dsl_auto
description: "DSL AUTO"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2
  image_picker: ^1.0.7
  sqflite: ^2.3.0
  sqflite_common_ffi_web: ^0.4.0 
  path: ^1.8.3
  provider: ^6.1.2
  flutter_local_notifications: ^17.0.0
  timezone: ^0.9.2
  fl_chart: ^0.68.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
